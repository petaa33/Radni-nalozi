const BaseHeader = ({children}) => {
  return (
    <div style={{color: "#14C38E", fontSize: "2rem", fontWeight: 600, margin: "20px 0", textTransform: "uppercase", borderBottom: `solid 6px #E3FCBF`}}>{children}</div>
  )
}

export default BaseHeader